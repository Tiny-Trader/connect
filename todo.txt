# tt-connect Production Hardening TODO (Source of Truth)
# Current state: 256 tests, lint + mypy clean, 79% coverage — 2026-02-27


# ── Completed (≤ 0.1.0) ───────────────────────────────────────────────────────

- [x] Release governance: semver/release policy, changelog workflow, tag-based publish pipeline.
- [x] Trust/compliance files: LICENSE, SECURITY.md, CODEOWNERS.
- [x] Compatibility matrix: CI test job runs on matrix [3.11, 3.12].
- [x] Typed configuration: AngelOneConfig + ZerodhaConfig Pydantic models, extra="forbid".
- [x] Operational resilience: PlaceOrderRequest `tag` is UUID4 auto-generated and reused as broker idempotency tag/uniqueorderid.
- [x] DB lifecycle: SCHEMA_VERSION versioning, CHECK constraints, exchange enum validation.
- [x] Historical OHLC data (both brokers). Zerodha oi=1 sent by default.
- [x] Zerodha market quotes REST: get_quotes → list[Tick], bid/ask from depth.
- [x] Broker live-test coverage: AngelOne (profile, funds, holdings, positions, orders, OHLC).
- [x] API cross-verification against official docs (both brokers):
      Zerodha: ERROR_MAP (9 types), _ORDER_STATUS_MAP (13 statuses) completed.
      AngelOne: totalsellavgprice typo, modify params, avg price fallback all fixed.


# ── 0.2.x — Observability + early correctness fixes ──────────────────────────

- [ ] Structured logging: log every outbound request and response envelope
      (broker, method, endpoint, latency ms, status). Use Python stdlib logging
      so callers can configure handlers. Include a correlation ID per request.
      Plan documented in docs/STRUCTURED_LOGGING_PLAN.md.

- [ ] AngelOne position ltp/pnl: verify live whether the API returns ltp,
      urealised, realised. If absent, mark them Optional in Position or remove.

- [ ] Subscription limit enforcement: raise ValueError before sending oversized
      requests. Zerodha: max 500 instruments per /quote, max 3000 per WS session.


# ── 0.3.x — Reliability (keep a long-running process alive) ──────────────────

- [ ] Session auto-refresh: tokens expire daily on both brokers. Background task
      (or 403-intercept) should refresh before the process starts failing.

- [ ] Retry + exponential backoff: NetworkException / 502 / 503 / 504 should
      retry with backoff. Order placement must NOT auto-retry (idempotency key
      handles dedup if the caller retries manually).

- [ ] WebSocket auto-reconnect: reconnect loop with backoff in BrokerWebSocket
      base class. Re-subscribe to all previously subscribed instruments on reconnect.


# ── 0.4.x — Feature additions ────────────────────────────────────────────────

- [ ] Rate limiting: token-bucket per adapter, transparent to caller.
      Zerodha: /quote = 1 req/s, /historical = 3 req/s, orders = 10 req/s.

- [ ] AngelOne Order Status WebSocket: order/trade update event stream.
      WS infrastructure exists; needs on_order_update callback wired up.

- [ ] Zerodha order varieties: AMO, Cover Orders, Iceberg, Auction.
      Needs optional variety field on PlaceOrderRequest.


# ── 1.0.x — Public production ────────────────────────────────────────────────

- [ ] Security hardening in CI: dependency vulnerability scans, secret scanning, SAST.
- [ ] User-facing documentation: quickstart, API reference, examples.


# ── Out of Scope ──────────────────────────────────────────────────────────────

- [-] Position Conversion (both brokers) — switch MIS <-> CNC mid-day
- [-] Margin Calculator (both brokers) — pre-trade margin estimate
- [-] Alerts, Mutual Funds, EDIS, Brokerage Calculator, Postbacks / Webhooks
